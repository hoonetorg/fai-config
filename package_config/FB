PACKAGES install
podman

PACKAGES install DEBIAN && GRUB_EFI
shim-signed
${grubefisignedpkgname}

PACKAGES install DEBIAN
lvm2
dosfstools
btrfs-progs
ntfs-3g

cryptsetup
cryptsetup-initramfs

#PACKAGES install DEBIAN && FBTRIXIE
# download this file alone so that faimirror can fail on this package but all other packages are downloaded
PACKAGES install DEBIAN
systemd-cryptsetup
